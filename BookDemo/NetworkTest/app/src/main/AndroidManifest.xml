<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.practice.networktest"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".AnalysisXML"></activity>
        <activity android:name="com.practice.networktest.OfficialDemo"></activity>
        <activity android:name="com.practice.networktest.MainActivity" />
        <activity
            android:name="com.practice.networktest.OkHttp"
            android:launchMode="singleTask" />
        <activity
            android:name="com.practice.networktest.PostSome"
            android:launchMode="singleTask" />
        <activity
            android:name="com.practice.networktest.BaseActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>